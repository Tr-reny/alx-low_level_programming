#include <string.h>
#include "main.h"

/**
 * _strlen - prints the length of a string
 * @s: string variable
 *
 * Return: length of the string
 *
 * Created by: Sanctus-Peter
 * cc: 12th july, 2022
 */

int _strlen(char *s)
{
	return (strlen(s));
}
